groups:
  - name: accounts-service.kafka
    rules:
      - alert: KafkaNotificationPublicationFailures
        expr: sum(increase(bank_kafka_publication_failures_total{application="accounts-service"}[15m])) > 0
        for: 1m
        labels:
          severity: warning
