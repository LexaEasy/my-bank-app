fullnameOverride: bank-gateway

image:
  repository: my-bank-gateway

service:
  port: 8080

management:
  enabled: true
  port: 8090

probes:
  port: management

env:
  MANAGEMENT_SERVER_PORT: "8090"
