fullnameOverride: exchange-service

image:
  repository: my-bank-exchange-service

service:
  port: 8086

management:
  enabled: true
  port: 8096

probes:
  port: management

env:
  MANAGEMENT_SERVER_PORT: "8096"
