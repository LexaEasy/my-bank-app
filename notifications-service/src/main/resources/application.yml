server:
  port: 8084

spring:
  application:
    name: notifications-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
