spring:
  cloud:
    config:
      enabled: false
  security:
    oauth2:
      client:
        registration:
          transfer-service:
            provider: keycloak
            authorization-grant-type: client_credentials
            client-id: transfer-service
            client-secret: test-secret
        provider:
          keycloak:
            token-uri: http://localhost:8180/realms/bank-realm/protocol/openid-connect/token
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/bank-realm
          jwk-set-uri: http://localhost:8180/realms/bank-realm/protocol/openid-connect/certs

bank:
  services:
    accounts:
      base-url: http://accounts-service
    notifications:
      base-url: http://notifications-service
    blocker:
      base-url: http://blocker-service
    exchange:
      base-url: http://exchange-service

eureka:
  client:
    enabled: false
